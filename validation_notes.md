# 验证记录

## 2026-08-21：问事页交互

在实际网页预览中确认：主页标题、显著免责声明、问题输入框及三组 1–999 数字输入框均能正常显示；测试问句与数值 17、23、5 均可写入并成功触发“启动三牌问事”提交。

提交后页面成功渲染三张牌（闪·逆位、五谷丰登·正位、闪电·正位）、牌面类别/花色/点数/效果、仪式摘要、本卦天山遁、互卦天风姤、变卦火山旅，以及体用生克说明。

流式解读完成后，以“塔罗·三牌叙事”“梅花易数·卦象脉络”“六壬意象旁注”“综合结论·可尝试的下一步”分节呈现；内容围绕同一组三牌和卦象交叉关联。“六壬意象旁注”明确说明非传统完整大六壬排盘，且严格免责声明在解读和操作区均可见。未登录状态下结果页正确显示“登录后归档”。

## 终端版重构验证

新版本已显示 7-Zip 式三栏布局：左侧文字导航、中间输入与规则表、右侧状态栏。默认解读器为“规则本机模式（无需模型）”，说明文字明确写明不联网、不调用模型；历史记录和归档入口已不再出现。已输入测试问题，下一步将提交三数并检查终端式结果区。

测试问句“我该如何安排今天的工作？”与数 A=17、B=23 已正确写入终端式表单，默认模式保持为规则本机模式。

已在规则本机模式下完成一次问事：结果区展示原创文字化卡牌桃（正位）、孙权（正位）、杀（逆位），每张牌包含类别、花色/体力、效果或技能与正逆位。终端式结果区同时显示本卦天山遁、互卦天风姤、变卦火山旅、体用关系，以及“未调用任何大模型”的规则模式通知和模板化解读；页面无历史或归档入口。

## 本地卡图导入接口验证

已验证 Electron 素材清单处理：仅接受 `game|general|identity|health-###` 实体牌 ID、PNG/JPG/JPEG/WEBP 相对路径，并阻止 `../` 路径越界；缺失图片会回退为文字卡面。卡面组件测试确认本地图片 URL 被加载时使用完整图片卡面，并在逆位时将卡片容器整体施加 `rotate(180deg)`，因此牌图、卡框和角标均倒着展示。

## 逆位视觉规则调整

已移除卡面上的正位、逆位和倒着状态标签。文字回退卡与本地图片卡都只在逆位时将整个卡片容器 `rotate(180deg)`，正位保持原方向。自动化测试同时覆盖文字卡、图片卡和跨多个仪式种子的正逆位方向分布，防止方向逻辑退化为全部正位。

直接对本地问事接口以同一问句连续采样，已得到 `upright/reversed/reversed`、`reversed/reversed/reversed` 等不同方向组合；网页结果区确认不再输出正位、逆位或倒着标签。Electron 冒烟测试则以最小素材包加载实际 PNG，并确认隔离渲染层能通过本地 `taluosha-asset://` 协议读取图片。结合图片卡组件的逆位旋转测试，确认本地图片与文字回退卡的逆位均仅由整张卡旋转表达。

## 独立随机方向端到端核验

已撤销“每组三牌至少一张逆位”的兜底。最新实际问事结果的 DOM 显示：第 1 张为 `data-orientation="reversed"` 且带有 `oracle-card--reversed`，第 2 张为 `upright`，第 3 张为 `reversed` 且带有同一倒置类。因此方向字段从服务端抽牌结果正确传递到前端卡面；先前预览截图未显出倒转，是预览捕捉对变换效果的呈现限制，不是抽牌引擎始终生成正位。

## 逆位实际显示故障修复

用户报告连续十次未观察到逆位后，重新在网页预览执行完整问事，输入“验证逆位显示是否正常”及数字 `111`、`222`、`333`。结果显示仪式摘要 `dfe63d4b4055c331` 和方向审计行 `[DIRECTION] #1:R #2:R #3:U`。

已定位根因为类名不一致：组件在逆位时附加的是 `oracle-card--reversed`，但样式只定义了 `sgs-card--reversed`，导致服务端已经返回 `reversed` 时页面仍不会命中旋转样式。现已把组件和样式统一为 `sgs-card--reversed`，并使用 `transform: rotate(180deg)` 与 `transform-origin: center center` 对整张卡面进行倒置。

修复后的同次实际网页结果中，第一张“决斗”和第二张“甘宁”均随 `R` 方向整张倒置，第三张“陆逊”随 `U` 方向保持正向；方向审计行、DOM 数据属性和视觉显示一致，且卡面没有添加任何可见正逆位文字标签。随后执行 `pnpm test` 与 `pnpm check`：5 个测试文件、14 条断言全部通过，TypeScript 类型检查无错误。

## 逐张方向追踪验证

为避免仅凭视觉判断，结果区现增加 `[TRACE]` 行，逐张输出 `S`（服务端响应）、`C`（客户端传入卡面组件）、`D`（卡片实际 `data-orientation`）和 `CSS`（是否命中 `sgs-card--reversed`）四项状态。

网页预览以问句“复核方向追踪”及数字 `444`、`555`、`666` 完成实测，仪式摘要为 `e78862539037f7b8`。追踪输出为：`#1:S=R/C=R/D=R/CSS=Y`、`#2:S=U/C=U/D=U/CSS=N`、`#3:S=R/C=R/D=R/CSS=Y`。对应第一、三张卡面实际整体倒置，第二张保持正向，说明三条方向数据链路与最终 CSS 类均一致。

网页预览的实际页面截图同时显示第一、三张完整文字卡面倒置、第二张保持正向；这与同页 `[TRACE]` 的 `R/Y` 与 `U/N` 状态逐张相符。网页端视觉证据用于核对可见方向，Electron 全界面测试则另外以计算 `transform` 样式验证桌面端旋转已经由渲染引擎实际应用。

新增的卡面诊断单元测试覆盖逆位 `clientOrientation`、DOM `dataOrientation` 与倒置类命中。最新 `pnpm test` 为 5 个测试文件、15 条断言全部通过；`pnpm check` 无错误。

## Electron 桌面渲染验证

新增全界面 Electron 冒烟测试后，在桌面渲染器中实际加载应用、填入问句与三个数字并提交抽牌。首次抽牌即得到追踪结果：`#1:S=U/C=U/D=U/CSS=N`、`#2:S=R/C=R/D=R/CSS=Y`、`#3:S=R/C=R/D=R/CSS=Y`。

测试同时读取逆位卡片的计算 `transform` 样式，并确认其不为 `none`；因此桌面端的文字回退卡不仅方向数据与 CSS 类一致，浏览器引擎也实际应用了 180° 倒置变换。命令以 `Electron reversed-card smoke test passed on draw 1` 成功结束。

## Electron 最小素材包与图片卡验证

桌面端全界面冒烟测试已扩展为创建临时最小素材包、写入 `manifest.json`、调用真实 `loadAssetPack` 校验、通过 `taluosha-asset://` 协议映射图片，并把 108 个游戏牌实体 ID 映射到同一张最小 PNG 测试图。

在 Electron 中实际加载该素材包并连续提交抽牌后，第 2 次抽牌得到 `#1:S=R/C=R/D=R/CSS=Y`、`#2:S=R/C=R/D=R/CSS=Y`、`#3:S=R/C=R/D=R/CSS=Y`。三张均为本地图片卡，均命中倒置类，且计算 `transform` 非 `none`；测试以 `Electron reversed-card smoke test passed on draw 2` 成功结束。这验证了 manifest 校验、本地导入映射、桌面渲染和整张图片卡倒置处于同一有效链路。

扩展后的双向测试会连续抽牌，直到同时见到正位与逆位本地图片卡；它断言正位图片卡的 `data-orientation` 为 `upright`、不带 `sgs-card--reversed` 且计算 `transform` 为 `none`，逆位图片卡则为 `reversed`、带倒置类且计算 `transform` 非 `none`。最新一次首次抽牌即得到 `#1:S=R/C=R/D=R/CSS=Y`、`#2:S=U/C=U/D=U/CSS=N`、`#3:S=U/C=U/D=U/CSS=N`，双向断言成功通过。

## 原创实体牌风格卡框改造

文字回退卡已改用原创的实体牌式结构：三层金色旧纸外框、类别顶栏、花色或势力角标、居中牌名签、带对角饰的插画窗、规则说明区和底部署名。按类别切换绿、红、朱色系，但没有复用官方插画、标识或卡面文件。

网页预览以问句“新版卡框是否清晰？”和数字 `271`、`528`、`836` 实测，得到一张倒置的郭嘉与两张正向的大乔、杀。标题签、角标、插画窗、说明区和底部署名均正常显示；`[TRACE]` 同时显示倒置牌为 `S=R/C=R/D=R/CSS=Y`，两张正位牌为 `S=U/C=U/D=U/CSS=N`。随后执行单元测试、类型检查及 Electron 本地图片冒烟测试，均通过。

Electron 冒烟脚本新增 `460px` 窄窗口校验：要求 `.card-grid` 为单列，卡片右侧不得超出视口，页面不得产生横向滚动。实测通过，且在窄窗口截图中可见新版卡框的多层边饰与完整单列卡面起始区域；同次追踪为 `#1:S=R/C=R/D=R/CSS=Y`、`#2:S=U/C=U/D=U/CSS=N`、`#3:S=U/C=U/D=U/CSS=N`。因此正逆位旋转和单列响应式布局均保持有效。
